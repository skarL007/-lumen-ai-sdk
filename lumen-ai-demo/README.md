# Legacy Visual Source

`Dashboard.tsx` is the source for the old static portfolio simulation. It uses
mock data and is not proof of SDK behavior.

Use `examples/local-observability-demo` for the real no-key demo backed by
LumenAI processors and Redis.

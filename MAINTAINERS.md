# Maintainers

Current maintainer:

- [skarL007](https://github.com/skarL007)

## Release Responsibilities

- Keep package versions, runtime `__version__`, and `CHANGELOG.md` aligned.
- Run `python scripts/release_gate.py` before publishing.
- Publish only the three package directories under `packages/`.
- Use GitHub security advisories for vulnerability coordination.
